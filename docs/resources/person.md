---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kanidm_person Resource - kanidm"
subcategory: ""
description: |-
  Manages a Kanidm person account.
  Authentication Setup
  Kanidm supports two credential setup workflows:
  Password-Based Authentication
  Set the password attribute to create a password-based account:
  
  resource "kanidm_person" "example" {
    id          = "jdoe"
    displayname = "John Doe"
    password    = var.initial_password
  }
  
  Passkey/Modern Authentication (Recommended)
  Set generate_credential_reset_token = true to generate a one-time token for credential setup via the Kanidm web UI:
  
  resource "kanidm_person" "example" {
    id                            = "jdoe"
    displayname                   = "John Doe"
    generate_credential_reset_token = true
  }
  
  output "credential_reset_token" {
    value     = kanidm_person.example.credential_reset_token
    sensitive = true
  }
  
  The user can then visit the Kanidm web UI with the token to set up passkeys or passwords.
---

# kanidm_person (Resource)

Manages a Kanidm person account.

## Authentication Setup

Kanidm supports two credential setup workflows:

### Password-Based Authentication
Set the `password` attribute to create a password-based account:

```hcl
resource "kanidm_person" "example" {
  id          = "jdoe"
  displayname = "John Doe"
  password    = var.initial_password
}
```

### Passkey/Modern Authentication (Recommended)
Set `generate_credential_reset_token = true` to generate a one-time token for credential setup via the Kanidm web UI:

```hcl
resource "kanidm_person" "example" {
  id                            = "jdoe"
  displayname                   = "John Doe"
  generate_credential_reset_token = true
}

output "credential_reset_token" {
  value     = kanidm_person.example.credential_reset_token
  sensitive = true
}
```

The user can then visit the Kanidm web UI with the token to set up passkeys or passwords.

## Example Usage

```terraform
# Example: Person account with password-based authentication
resource "kanidm_person" "alice_password" {
  id          = "alice"
  displayname = "Alice Smith"
  mail        = ["alice@example.com"]
  password    = var.alice_password

  lifecycle {
    # Ignore password changes after creation (managed externally)
    ignore_changes = [password]
  }
}

# Example: Person account with passkey/modern authentication (recommended)
resource "kanidm_person" "bob_passkey" {
  id                              = "bob"
  displayname                     = "Bob Johnson"
  mail                            = ["bob@example.com"]
  generate_credential_reset_token = true
  credential_reset_token_ttl      = 7200 # 2 hours
}

# Output the credential reset token for Bob
output "bob_credential_reset_token" {
  description = "One-time token for Bob to set up credentials via Kanidm web UI"
  value       = kanidm_person.bob_passkey.credential_reset_token
  sensitive   = true
}

# Example: Person account without initial credentials
resource "kanidm_person" "charlie" {
  id          = "charlie"
  displayname = "Charlie Brown"
  mail        = ["charlie@example.com", "cbrown@example.com"]
}

# Example: Imported existing person account
# Import command: tofu import kanidm_person.existing_user username
resource "kanidm_person" "existing_user" {
  id          = "existing"
  displayname = "Existing User"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `displayname` (String) Display name of the person.
- `id` (String) Unique identifier for the person account (username). Cannot be changed after creation.

### Optional

- `credential_reset_token_ttl` (Number) Time-to-live for the credential reset token in seconds. Defaults to 3600 (1 hour).
- `generate_credential_reset_token` (Boolean) Whether to generate a credential reset token for passkey/password setup via the web UI. Mutually exclusive with `password`. Defaults to `false`.
- `legalname` (String) Legal name of the person (full legal name as it appears on official documents).
- `mail` (List of String) Email addresses for the person.
- `password` (String, Sensitive) Password for the person account. **Note:** This is write-only and will not be stored in state. Mutually exclusive with `generate_credential_reset_token`. Consider using `lifecycle { ignore_changes = [password] }` if the password is managed externally.
- `unix_gid` (Number) Unix GID number for Linux/PAM authentication. Enables Unix account integration.
- `unix_shell` (String) Login shell for Unix/PAM authentication (e.g. `/bin/bash`). Requires `unix_gid` to be set.

### Read-Only

- `credential_reset_token` (String, Sensitive) The credential reset token (generated when `generate_credential_reset_token` is `true`). This token can be used once to set up credentials via the Kanidm web UI. **Computed value only.**
